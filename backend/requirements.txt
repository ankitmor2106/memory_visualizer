# Core API
fastapi==0.111.0
uvicorn[standard]==0.29.0
uvloop==0.19.0          # faster event loop (Linux only; gracefully ignored elsewhere)
pydantic==2.7.1

# JavaScript tracer — CDP over WebSocket
websockets==12.0

# C++ tracer — GDB MI subprocess control
pexpect==4.9.0
